//Solicite ao usuário para inserir um número representando um dia da semana (1 a 7) e mostre o nome do dia correspondente.
import data from 'readline-sync';

let dia =data.question(" informe o dia da semana que vc deseja:")
switch (dia) {
    case value:
        
        break;

    default:
        break;
}
